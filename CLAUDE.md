# Delray.services / TimeBroker.ai — Claude Code Project Memory

> **Inherits:** the vault brain at `H:\SovereignCloud\CLAUDE.md` (stack mandate, OMNI-PATH, IP firewall, agent topology) loads automatically. Canonical multi-agent standard: `…\luxurymarinelife-main\AGENTS.md`. This file is the site-specific quick-load.

## At a glance
- **Tier / entity:** 1 — Personal IP (Delray Wannemacher, sole proprietor). DBAs: **TimeBroker.ai**, **Delray.services**. Owns all tech IP; licenses to the for-profit.
- **Live:** https://delray-services.web.app and https://timebroker-ai.web.app — Firebase Hosting, project `ac-godmode-titan`
- **Stack:** Static HTML/CSS/JS landing + launch system; Firebase Cloud Functions; Gemini chatbot (`ai-chatbot.js`), viral engine, LinkedIn toolkit, booking widget, admin dashboards
- **Deploy:** `firebase deploy --only hosting` (targets `delray-services` + `timebroker-ai`)

## Site-specific notes
- **Active rebrand:** Delray.Services → **TimeBroker.ai** (messaging migration in progress across both hosting targets).
- Integrations: Bitrix24 CRM webhooks, Google Calendar booking. Config in `config.js`.
- Fractional CAIO / capital-strategy positioning ("Give Back Challenge": 50% rate + 10% pledge).

## Cohesive system active here
Global Superpowers skills + the 5-agent Swarm (`~/.agents/config.json`) + shared MCP set (`.mcp.json`) apply in this repo. Knowledge → `.agent/knowledge/`. Google-native only.
